{
  "name": "aureus",
  "version": "0.8.2",
  "description": "Aureus - Advanced Git automation plugin for Claude Code",
  "license": "MIT",
  "scripts": {
    "postinstall": "node hooks/install-dependencies.js && echo \"✓ Aureus plugin installed - Run 'npm run start-web' to start the web server\"",
    "start-web": "cd web && npm start",
    "start-mcp": "cd mcp && npm start",
    "clean-cache": "node hooks/clean-cache.js",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0"
  }
}
